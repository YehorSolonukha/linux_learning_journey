1. discover bandit26's login shell path:
cat /etc/passwd | grep bandit26
2. read the contentes of a shell script:
cat /usr/bin/showtext
3. it runs `more` command -> if terminal is shrinked we can linger in the terminal windows:
shrink the terminal to a few lines, copy the sshkey from bandit25 home dir and with it ssh into bandit26
4. run vim with `v` key, set shell to /bin/bash:
type: `:set shell=/bin/bash`
type: `shell` 
5. read password for bandit27:
./bandit27-do cat /etc/bandit-pass/bandit27

password for lvl28: upsNCc7vzaRDx6oZC6GiR6ERwe1MowGB 